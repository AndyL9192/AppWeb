export const button = ({ texto = "default", variante = "blue" }) => {
  if (variante === blue) clase = "btn-red";
  if (variante === green) clase = "btn-red";

  if (variante === red) clase = "btn-red";
};
